<% @games.each do |game| %>
  <div class="booyah-box col-10 offset-1">
     <h1><%= link_to game.name, search_page_path(game) %></h1><br />
    <p><%= game.description %></p><br />
    
    <% if game.user.present? %>
      <small><%= game.user.email %></small>
    <% end %>
  </div>
<% end %>
